# PrivateInstanceGenerator

## Prerequisites
```
sudo apt install libboost-all-dev
```

## TODO
* TIMEOUT specified by the api. Different challenges have different timeouts [major]
* Check for memory corruptions, code is heavily GPT generated
* API Key for auth
